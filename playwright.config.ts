import { defineConfig } from '@playwright/test';
// Target another local DEMO instance (for example pnpm competition:start --isolated on 3001) with BRANDOPOLIS_BASE_URL and BRANDOPOLIS_SESSION_FILE.
const baseURL=process.env.BRANDOPOLIS_BASE_URL??'http://127.0.0.1:3000';
export default defineConfig({testDir:'./tests/browser',timeout:60000,workers:1,reporter:'list',use:{baseURL,channel:'chrome',trace:'off'},projects:[{name:'wide',use:{viewport:{width:1600,height:1000}}},{name:'desktop',use:{viewport:{width:1440,height:900}}},{name:'compact',use:{viewport:{width:1280,height:800}}},{name:'tablet',use:{viewport:{width:768,height:1024}}},{name:'mobile',use:{viewport:{width:390,height:844},isMobile:true,hasTouch:true}}],webServer:{command:'pnpm dev',url:baseURL,reuseExistingServer:!process.env.CI,timeout:30000}});
